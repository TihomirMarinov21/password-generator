# Password Generator

This Python program generates random passwords based on user input.

## Features:
- Allows the user to choose the complexity (letters only, letters + digits, or complex).
- Option to choose the length and amount of passwords.
- Simple and user-friendly interface.

## Usage:
Run the script and follow the prompts to generate passwords.

## Requirements:
- Python 3.13.9
